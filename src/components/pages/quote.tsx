import React from "react"
import Hero from "~/components/configurable/Hero"

const QuotePage = ({ location }) => {
  const { state = {} } = location

  return <div className="quote-page"></div>
}

export default QuotePage
